# Decision log

Append-only, dated, past tense by construction. One row per stage transition,
never per activity. This file records what was decided and where the evidence
lives; it never describes current behaviour, so it cannot drift and must never
be patched. Current truth lives in exactly three places: the code, the tests,
and the stamped docs. Anything else here is history.

The workflow this file belongs to (adopted 2026-08-28): proposals carry their
judgment half as a paragraph in the relevant planning doc and their measurable
half as acceptance assertions, armed at spike stage; promotion renames the
acceptance file into the permanent suite; agent reports and audit verdicts
travel in commit messages; retired documents are deleted and retrieved through
git history, not kept live to rot.

| date | decision | evidence / where |
| --- | --- | --- |
| 2026-08-28 | Posture: pre-v2 maturation recorded by the decision owner. The RC window is the runway; the freeze is evidence pressure (the v1.17.0 template: measured cost, what it buys, fixtures), not a calendar. Supersedes the park-to-stable defaults in earlier drafts. | docs/plan-vapor-composition.md section 7 |
| 2026-08-28 | Vapor outlet Phase 1 spike PASSED: -20.13 KB brotli vs a re-derived interop baseline against a >= 20 KB accepting bar, least favourable of four method variants, four behaviours passing against an executed production build, no interop binding retained. Audited and reproduced run-for-run. | superseded spike (git history); method survives in tests/vapor/vapor-outlet-size.test.ts |
| 2026-08-28 | Vapor outlet promoted: vapor-chamber/router/vapor ships experimental in the next minor. One additive error code (mode_mismatch). Shipped margin 20.03 KB over the bar; the per-run guard fails loudly on erosion and a failure is a decision trigger, not a threshold to raise. Two recovery levers measured and DECLINED at acceptance (plain Error for routerError, ~77 B; dropping the DEV gate, ~35 B), recorded in the guard's header. | src/router/vapor.ts; tests/vapor/vapor-outlet*.test.ts; tests/router/vapor-boundary.test.ts; ROADMAP version-targets row; CHANGELOG Unreleased |
| 2026-08-28 | Store direction: a Pinia-like store is rc-independent (stable 3.5-era API or the signal shim) and 1.x-eligible under the maturation posture, gated on a deliberate reversal of whitepaper section 6 recorded in the same commit that documents the partition rule. Pinia 4.0.3 read at source: a packaging major (ESM-only, devtools-api v8, Nostics), no Vapor or signals work; patterns adopted by reference, its action() mini-bus replaced by the real bus. | docs/plan-vapor-composition.md sections 3, 4, 7, 8 |
| 2026-08-28 | Packaging: a separate satellite package for the outlet (and any future store) considered and REJECTED; cost isolation is an import-graph property the subpath already delivers, and semver quarantine already exists in-package (experimental status, v1.11.0 precedent). | docs/plan-vapor-composition.md section 6 |
| 2026-08-28 | Style: ASCII-only typography adopted project-wide by the owner (no em/en dashes, arrows, ellipsis, times sign, curly quotes). Measured honestly: 245 B raw / ~95 B compressed across dist strings, ~6 KB npm tarball; the rule is the primary reason, the bytes are the dividend. Guard exists; the one-pass sweep and lint:check wiring are pending. | scripts/check-ascii.mjs |
| 2026-08-28 | Workflow: git adopted as the substrate (baseline 30edfae); this decision log adopted in the whitepaper alignment-log form; acceptance-assertions-at-spike adopted as the RFC replacement for measurable claims. The outlet RFC, fully promoted, is deleted in the commit following this row and retrievable from git history. | this file; git log |
| 2026-08-31 | Size metric: a second splitting-aware pass adopted in measure-size.mjs (first load / on demand columns, attribution by import-statement reachability from the entry chunk, not esbuild's entryPoint flag - which is stamped on dynamic-import chunks and would invert the answer). Historical single-file column retained unchanged for cross-release comparability. Trigger: the no-splitting pass inlines internal dynamic imports plus wrapper, charging a penalty for deferral. | scripts/measure-size.mjs; docs/BUNDLE-SIZES.md |
| 2026-08-31 | Router http client removed from the core: http and fetchBlade become ordinary options, the in-box pair ships as vapor-chamber/router/remote (routerHttp, bladeFetcher; bladeRoot moved onto the fetcher). Breaking for { url } tables and blade rows, two-line migration, taken now under the maturation posture - the RC window is the runway, and the cost of this break only rises. An intermediate lazy-import variant was measured and superseded in the same session: it fixed startup cost but left the dependency in the graph and the decision with the callee. ./router 12.4 -> 9.6 KB brotli; boundary enforced like the renderer boundaries. | src/router/remote.ts; tests/router/remote-boundary.test.ts; docs/router.md; CHANGELOG Unreleased |
| 2026-08-31 | revalidateRoutes stage 1 (acceptance before implementation): criteria committed as tests/revalidate-routes.test.ts - 6 assertions on the composition surface today, 13 it.todo armed at implementation. PLACEMENT decided bus-side with the router passed STRUCTURALLY: src/router imports nothing from the bus and src/index.ts nothing from the router, so a router-adjacent subpath would create a router -> bus edge that does not exist, to host a plugin installed on the bus. REVALIDATING decided as the plugin's own signal, which CORRECTS a plan claim: section 4C says "isRevalidating drives the UI for free" and it cannot - router.isRevalidating is Readonly and its only writer, the engine's trackRevalidation, is absent from the Router type, so driving it needs a cast or new router surface, both out of scope by the brief. | tests/revalidate-routes.test.ts; docs/plan-vapor-composition.md section 4C |
| 2026-08-31 | revalidateRoutes shipped (experimental, next minor): a bus plugin composing runLoaders + currentRoute + setRouteData, zero new router capability. THREE decisions revised by evidence rather than argued: placement moved bus-side -> router-side at stage 2 (runLoaders has no public substitute, the bus contributes one inline function shape - deep one way, shallow the other); then subpath -> the EXISTING router entry, because minting router/revalidate added a Vite build entry that re-chunked shared code and dropped the outlet guard 20.02 -> 19.90 with the outlet itself unchanged, and a subpath isolates no cost when the module imports nothing the core lacks. `loaders` is an explicit argument: createRouter closes over its preset, so a two-arg form would run a second http client and cache. | src/router/revalidate.ts; tests/revalidate-routes.test.ts; docs/router.md |
| 2026-08-31 | Guard-and-core review pass, restraint applied as review rather than addition. Findings: check-line-citations was blind to 73 of 2524 test titles (double/backtick quotes, .each/.skipIf chains) and the hole was LIVE - six stale citations were sitting behind it, each in a double-quoted title precisely because the title quoted a string. BUNDLE-SIZES promised hard ceilings over the whole document; check-size budgets 3 IIFE files and none of the 24 ESM rows, which is why size:check stayed green while ./router moved 12.4 -> 13.1 -> 9.6. command-bus.ts's header quoted ~3.6 KB against a measured 4.2, and CommandMeta.id documented crypto.randomUUID as the default when the default is a monotonic counter - contradicting the ts field eight lines above it. Guards are now believed only as far as their scope, which is stated. | scripts/check-line-citations.mjs; scripts/measure-size.mjs; src/command-bus.ts |
| 2026-08-31 | Prose guards adopted: scripts/check-doc-claims.mjs wired into lint:check (orphaned docblocks, documented defaults whose literal is absent from the file, release status in comments - the last per the owner rule that release state belongs to CHANGELOG.md and a bare version number is enough in source). check-ascii extended to invisible characters (ZWSP, word joiner, BOM, NBSP; joiners excluded on biome noMisleadingCharacterClass). Trigger: a read pass found 14 defects, all in prose and none able to fail a test; the docblock check was found by reading one file and then caught two more by sweep, one of them in a passage read attentively the same hour. Scope stated in the header: these check that a claim is ATTACHED and its literal EXISTS, never that it is true. | scripts/check-doc-claims.mjs; scripts/check-ascii.mjs; CONTRIBUTING.md |
| 2026-08-31 | Store stage 1 (acceptance before implementation), and a correction: Vue 3.6 stable was recorded as a GATE on the composition plan and it is a DEADLINE - section 7 posture is that the RC window is the runway, item 4 at stable is re-measure/promote/stabilize, and section 7 item 2 marks the store rc-independent. The store gate is one sentence of whitepaper section 6 about PACKAGE scope (a state layer as a fourth source of truth), not its architectural claim (the bus stores no state), which the store preserves. Stage 1 also falsified a section 3 pattern: injection-first resolution on hasInjectionContext() is unreachable (module-private in chamber.ts, no accessor) and undesirable (chamber reaches Vue by the runtime probe behind both shipped prod-only bugs) - the store imports from vue statically like src/router, which also settles it as a subpath. | tests/chamber-store.test.ts; docs/plan-vapor-composition.md |
| 2026-08-31 | Store shipped (experimental, next minor): defineChamberStore on the ./store subpath, importing vue and nothing else. The whitepaper section 6 reversal is recorded in section 6 itself and is PACKAGE scope only - the bus still stores nothing, a store owns its own shallowRef, and the competition argument is what failed: Pinia's action()/$onAction mini-bus exists because no bus is underneath it, so here the store is 0.6 KB of the existing plugin catalogue applied to state rather than a fourth source of truth. ONE decision revised by measurement: the bus was a DEFAULTED argument (getCommandBus()) and is now REQUIRED. The default contradicted both of its neighbouring docblocks - it imported chamber.ts, so the module written to avoid the runtime probe executed it at load, and it reached for the module global that the per-bus registry exists precisely to avoid, handing every SSR request the same stores. Invisible to a green suite; caught only by the module measuring 16.7 KB raw / 5.4 KB brotli against the section 2 estimate of 1-2 KB. Required: 1.4 KB raw / 0.6 KB brotli. The boundary test written to pin this was itself inert on first write - under happy-dom import.meta.url is an http://localhost URL, so existsSync answered false every run and the assertion could not fail; it is armed by injection now. url fields (4B) take the router structurally, so a store without them never pulls it into the graph. | src/store.ts; tests/chamber-store.test.ts; docs/store.md; docs/whitepaper.md section 6; CHANGELOG Unreleased |
| 2026-09-14 | Outlet size guard re-decided (rc.8 cycle): built with Vite's build API (rolldown, the consumers' bundler) instead of esbuild, entry chunk measured, and two stamped limits replace the interop-minus-vapor bar - own machinery <= 5.0 KB brotli (4.23), saving >= 15 KB (20.42). Trigger: the 19.5 bar failed on rc.8 at 19.43 under esbuild while rolldown measured rc.8 as a 1.57 KB improvement (clean trees: esbuild 20.06 -> 19.40, rolldown 18.85 -> 20.42); the difference metric had fired twice on improvements. | tests/vapor/vapor-outlet-size.test.ts; scripts/stamp-docs.mjs; whitepaper rc.8 row; CHANGELOG v1.20.0 |
| 2026-09-14 | H1(b) shipped: `vaporChamberWire()` from vapor-chamber/vite, a build-only plugin that resolves the bare `vapor-chamber` specifier to a virtual module (`import 'vapor-chamber/vue'` or `/vapor`; `export *` from the real root, resolved with skipSelf), so root imports are wired in production with no import changed. Measured on a real Vite build of a root-only consumer, vue external: from importing nothing from vue to the eight configureVue names plus pauseTracking/resetTracking, +554 B raw / +145 B brotli - exactly the static entry's wiring; nothing added to any app-facing library file. Build only: the dev server resolves the runtime lookup, and a dev-time redirect would pair a pre-bundled /vue with an unbundled root. The Vapor choice is a consumer option, not inferred from plugin-vue. | src/vite-hmr.ts; tests/vite-wire-plugin.test.ts; README "Which entry"; CHANGELOG v1.20.0 |
| 2026-09-14 | v-vc:command Vapor port shipped (D3): `vcCommandVapor` from vapor-chamber/directives, the `(el, value, argument, modifiers) => cleanup` shape over the same buildHandler; the install-time Vapor warning removed. ONE deviation from the planned shape, taken on a 3.5-compatibility constraint: the binding getter is read at dispatch time instead of tracked with `renderEffect`, because a static import of `renderEffect` (Vapor build only) would break the directives subpath at link time for Vue 3.5 consumers of the vDOM plugin; re-targeting on a changed binding is pinned either way. Cost: dist/directives.js 7,213 -> 6,831 B raw (1,894 -> 1,790 brotli), IIFEs and Blade bundle unchanged. | src/directives.ts; tests/directives-vapor-fixture.test.ts; whitepaper 10.3; ROADMAP; CHANGELOG v1.20.0 |
| 2026-09-14 | vaporChamberWire and the dev server (batch 3, D1), decided by measurement: the redirect stays build-only. On a real Vite dev server with the package installed and pre-bundled, a dev-time redirect put TWO chamber modules in the browser graph - the wired root's export * reached the pre-bundled root while its import of /vue, made from a module filed under node_modules, was served unbundled - against ONE for the documented bus-from-root + composables-from-/vue pattern, and one in every arm with the package linked. The H1(b) row above had the pairing reversed (a pre-bundled /vue with an unbundled root); this row supersedes that clause. Under serve the plugin instead defines __VC_WIRED__, which the DEV probe-path hint reads, so a plugin user is not told to change imports the build already wires. No version bump: 1.20.0 is itself unreleased, so this ships inside it (owner, 2026-09-14). | src/vite-hmr.ts; src/chamber.ts warnProbePath; tests/vite-wire-plugin.test.ts; CHANGELOG Unreleased |
| 2026-09-14 | Per-target loading shipped: `useSharedCommandState().isLoading(action, target?)`, a reactive boolean per `commandKey`, bus-wide, additive (existing return unchanged). ATOMIC by construction - one lazily created signal per key, written only on 0 <-> 1 - measured with sync-flush effects: a transition on one key never re-runs another key's reader. The start is a before-hook installed at the first isLoading() call (zero cost to a bus nobody asks), the settle folds into the existing on('*') observer. ONE handoff premise corrected by reading the runners: pairing was to rest on "a settle without a start only decrements, clamped at 0" - true for stuckness, but such a settle (pre-flight abort, query, emit, an earlier before-hook's throw) would clear a CONCURRENT dispatch's flag; matching by Command object (a WeakMap) ignores it instead. And one exit is NOT paired: a plugin that throws or rejects escapes buildRunner/buildAsyncRunner, so the start has no settle and that key stays true - pinned as a known limit, not fixed here (a bus contract question). Map bound: keys ever read (retained, 272.5 B each, Vue shallowRef backing, 100k-key heap probe) plus keys in flight; unread keys are pruned at 0 (37.6 B/key residual measured, source TO VERIFY). Cost: full IIFE 11,731 -> 11,931 B brotli; core, elements, Blade consumer byte-identical. | src/chamber.ts; tests/command-loading-fixture.test.ts; README; docs/performance.md; CHANGELOG v1.20.0 |
| 2026-09-15 | Plugin throw -> errResult shipped (owner-decided from the feature-loading audit): both runners convert a plugin's synchronous throw or rejected promise AT EACH PLUGIN'S BOUNDARY into a `VC_PLUGIN_THREW` result (cause = the original, context = the plugin's name and chain index), so the outer plugin's cleanup runs and after-hooks, on('*'), the error observer and isLoading all settle. One pipeline: no throw-in-dev/result-in-prod split; a DEV console.error names the plugin. Not retryable; circuitBreaker neither counts nor resets on it. `onMissing: 'throw'` passes through unchanged - found before coding, it crosses the chain from below by contract and a plain wrapper would have relabeled it. This resolves the plugin-throw clause of the row above; a second start-without-settle exit (`onMissing: 'throw'` with tracking on) was found and pinned. Cost: full IIFE 11,625 -> 11,736 B brotli (over the 11,700 budget, owner), core +114, elements +116, Blade consumer 6,118 -> 6,198; async "3 plugins + 1 listener" 1.14-1.18x after the pass-through short-circuit (1.33-1.40x wrapping every level), sync at the noise floor. | src/command-bus.ts; src/plugins-extra.ts; src/schema.ts; tests/plugin-throw-fixture.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s30 |
| 2026-09-15 | Plugin throw reworked, split and re-measured (owner review of s30). The speed figures in the row above came from an uncommitted scratch harness with no committed baseline and no self-control arm; they are superseded, and every figure here comes from tests/plugin-throw-ab.test.ts (three arms - baseline derived by reverting the change, byte-identical self-control, shipped - production, gc, three runs). Design kept after the alternatives were read against the docs: per-level conversion, because the `Plugin` type (`next: () => CommandResult`) and the README's canonical plugin (`const result = next(); ...; return result;`, no try/finally) promise that `next()` does not throw; top-level-only conversion would break both, and a sync/async hybrid contradicts whitepaper 5.5 ("the same model for sync and async buses"). Shape reworked: the first cut's shared tryPlugin/tryAsyncPlugin helpers gave both runners ONE plugin call site, megamorphic with distinct plugins; each runner now has its own inline try, and the async runner tracks the last level's value in one per-run slot so a pass-through plugin is not re-wrapped. Shipped vs baseline, "3 plugins + 1 listener": sync 1.004-1.023x, async 0.977-1.017x, inside or at the edge of the self-control band. DECLINED, both kept as arms: `try { return await ... } catch` per level (proposed in review) 1.494-1.541x on async "3 plugins" and 1.111-1.220x with no plugins - an async frame per level, the cost perf/2 already measured on `execute`; `.then` on every level 1.180-1.217x. `context` carries `index` only (the function name was 19 B brotli and '' for arrows; DEV logs it). The onMissing:'throw' stuck key is fixed in its own commit (plugin-throw/5): the dispatch settles, then re-throws, and only an onMissing:'throw' sync bus pays the frame. Budgets raised AFTER the squeeze, in one block of scripts/check-size.mjs with the per-piece table: full 11,700 -> 11,810 brotli and 39,800 -> 39,900 raw, elements 8,450 -> 8,480. Process note: the first cut of the committed A/B timed the src module against derived copies and read a settle cost that disappeared once every arm was loaded the same way. | src/command-bus.ts; src/chamber.ts; tests/plugin-throw-ab.test.ts; scripts/check-size.mjs; CHANGELOG v1.20.0; rc-alignment-work.md s30 |
| 2026-09-15 | Provenance note for the row above (appended, not edited): its figures come from THREE DISTINCT plugin functions per level and per bus type - the shape of a real chain. Every figure taken before the rework, the scratch five-arm "2-3% on async plugins" included, came from harnesses that installed one arrow at every level (fresh closures of a single function literal, one per bus type), a shape that keeps the plugin call site near-monomorphic. The two sets were measured under different call-site shapes and are not comparable: a move from one to the other is not an optimization. | tests/plugin-throw-ab.test.ts (SYNC_PLUGINS / ASYNC_PLUGINS); rc-alignment-work.md s30.8 |
| 2026-09-15 | `history()` recorded an async redo twice - the recorder runs when the dispatch settles, after `redo()`'s `finally` had cleared `_replaying` - and wiped the redo stack. Fixed with `_withOrigin('redo', ...)` on the redo dispatch and a `meta.origin` check in the recorder, the flag KEPT for the sync bus (an undo handler's own dispatch is a rollback and must stay out of the ledger). Every variant measured on the full IIFE; budget 11,810 -> 11,817 with the table in check-size.mjs; the committed A/B read both dispatch rows inside the control band. Owner: 1.20.0, landed first (q4/1, 587f4eb). | src/plugins-core.ts; tests/history-redo.test.ts; tests/history-redo-ab.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s33.1 |
| 2026-09-15 | `seal()` commits the ledger (owner: dispatch is the "do", the undo handler the rollback; the plugins stay in place). A sealed bus refuses `clear()` with VC_CORE_SEALED - it used to succeed, delete the undo handlers and every plugin, and stay sealed, so `history().undo()` rolled back nothing. `dispose()` still works on a sealed bus and leaves it sealed; the TestBus matches and carries `_UNSEAL` so `unsealBus()` reopens it. Owner: option B in 1.20.0 (q1/1, c1b049a). | src/command-bus.ts (seal JSDoc, clear sites); src/testing.ts; tests/seal-clear.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s33.1 |
| 2026-09-15 | `dispose()` settles waiting `request()`s as VC_CORE_ABORTED with their timers cleared (they used to resolve as a retryable timeout up to 5 s later and hold a Node process open), and the sync `request()` honours its caller's signal (its type declared one; its body read only `timeout`). Two measured cuts were rejected for breaking rules the code states (a `signal` on the sync command; cancels mixed into `throttleTimers`); the landed design follows them: one command shape pinned with %HaveSameMap, nothing on the dispatch path, a lazily-null `waiting` field, the async request's structure kept, the responder not told. Measured: sync request with a sync responder 0.39-0.41x, waiting on an async responder 1.06-1.09x, async request inside its band; the bus-owned-signal design (1.7-1.9x) declined; budgets to the measured bytes. Landed q2/1 (4114547) and q2/2 (f6f3aac, the aborted message no longer claims "before it ran"). | src/command-bus.ts; tests/request-dispose.test.ts; tests/request-dispose-ab.test.ts; tests/v8-shapes.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s33.1 |
| 2026-09-15 | A before-hook's throw is a VC_CORE_BEFORE_CANCEL result (owner: option A over the C recommendation - the code had been declared since v1.0 and never produced): the thrown value as `cause` and as the message, a thrown BusError passed through, emitter `hook`, severity `error` (the registry's `warn` dated from when nothing emitted it; BusSeverity defines `error` as a failed dispatch), no stack captured for the wrapper as `wrapThrottle` skips it - 2.2x on the cancelled path with the capture, 1.05-1.09x without, the non-throwing rows inside the band; the TestBus matches. Landed q3/1 (89060e5). | src/command-bus.ts (beforeCancel); src/schema.ts; src/testing.ts; tests/before-cancel.test.ts; tests/before-cancel-ab.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s33.4 |
| 2026-09-15 | Release closure of v1.20.0, owner: "any change is the moment before close the release". `alien-signals` became an optional peer (q5/1: the manifest is not a stable surface). `coverage:doc` counts the A/B files by calling vitest directly (q6/1; the recorded env-flag form declined, a shell-set variable breaks `npm run` on Windows). Every ESM size row is a Vite production build of `dist/` (q7/1: about 10% lower, a measurement change, the consumer row now the tree-shake gate's own number), with the Vapor wiring deltas (Vue bundled) and mitt as rows, so the README's wiring figures, the migration guide's mitt size and the store guide's size are stamps; the ROADMAP's 2,299 B / 885 B stays hand-typed as a dated decision figure. CI bench-ratio stamping declined: run-to-run ratio noise would have CI commit a stamp change on every push. The s31.3 comment-noise list is left to the owner. | package.json; scripts/measure-size.mjs; scripts/stamp-docs.mjs; docs/BUNDLE-SIZES.md; CHANGELOG v1.20.0; rc-alignment-work.md s33.4 |
| 2026-09-15 | v1.20.0 re-cut before the tag moved anywhere (owner: "what block you to fix s31.3 and finish cancelation on this release?", then "the rest of feature you mention can goes 1.20", "do all"). Landed on top of the first release commit: `dispose()` runs each installed plugin's `dispose()` and `retry()` ends its backoff sleeps as VC_CORE_ABORTED (cancel/1; the loop squeezed from a helper to an inlined optional call, -9 / -13 / -18 B); the TestBus's `request()`/`respond()` made real (cancel/1); one rule for what an undo handler or a redo dispatches - a scoped origin read in stampMeta, 'undo' / 'redo', skipped by the plugin and the composable on both buses (undo/1; the plugin's `_replaying` flag gone; the hot-path read at parity on the sync rows of a committed A/B); the s31.3 comment-noise list cut where it was a repeat or a paraphrase and kept where it was navigation (comments/1). The local tag v1.20.0 was deleted and re-created on the re-cut release commit; the first release commit stays in history. | src/command-bus.ts; src/plugins-io.ts; src/plugins-core.ts; src/chamber.ts; src/testing.ts; tests/dispose-plugins.test.ts; tests/testbus-request.test.ts; tests/undo-origin.test.ts; tests/origin-scope-ab.test.ts; CHANGELOG v1.20.0; rc-alignment-work.md s33.5 |
| 2026-09-21 | BACKFILLED, written with rc-alignment-work.md s34 rather than at the time - the rc.9 cycle took its decisions without recording them, and a row dated as if written during the work would be its own false claim. Vue 3.6.0-rc.9 #15490 made a Vapor custom directive's ARGUMENT a getter (`() => ("command")` where rc.8 emitted `"command"`), so `vcCommandVapor`'s `argument !== 'command'` guard stopped mounting: v-vc:command was a dead control in every compiled Vapor template, silently. Taken: the getter and ONLY the getter, not both shapes. Vue's own `VaporDirective` declares `argument?: () => Arg` with no union, so the string is not a legacy shape but an illegal one; every release that emitted it is outside the peer range (checked with semver: `>=3.5.0` admits no Vapor at all, since 3.5 ships neither runtime-vapor nor compiler-vapor, and the second arm starts at rc.9); and the string arm would be a branch no installable Vue can reach, provable only by a hand-written literal - the construct that hid this defect for a release. The DEV guard names the required version instead, and folds to nothing in a consumer's production build (measured in a Vite production bundle: neither the guard string nor the version string survives, no unfolded `typeof process` or `import.meta.env` left). A dynamic `v-vc:[kind]` LATCHES, on both renderers alike - measured on the vDOM plugin, where `updated` guards on `binding.arg !== 'command'` and returns - so it is documented rather than warned about. Cost: +117 B brotli on dist/directives2.js; all three IIFE variants byte-identical, as they carry no directives code. | src/directives.ts; tests/directives-vapor-fixture.test.ts; tests/compile-vapor.ts; CHANGELOG (pending, s34.7); rc-alignment-work.md s34 |
| 2026-09-21 | BACKFILLED with s34. Teardown is keyed to what was MOUNTED on the element, never to the current binding. The vDOM `v-vc`'s `beforeUnmount` guarded on `binding.arg !== 'command'`, and Vue passes the LATEST binding, so after a dynamic argument moved off `command` the guard returned and `unmountCommand` never ran. Honest scope, stated because the first draft of this overstated it: reachable only through `v-vc:[kind]`, which nothing uses; the delegated refcount is otherwise complete and correct - plain unmount, partial, full and double-unmount all measured correct BEFORE the fix. The fix is the deletion of that line; `unmountCommand` already no-ops on an element with no state, so the guard bought nothing. Net -56 B. The tests assert the CLEANUP, not the symptom: a test that only checked that clicking the old node no longer dispatches would keep passing if someone made the handler inert and left the listener attached. | src/directives.ts; tests/directives-vapor-fixture.test.ts; rc-alignment-work.md s34.3 |
| 2026-09-21 | BACKFILLED with s34. One listener-options object, recorded at mount and handed to both `addEventListener` and `removeEventListener`; the `capture` boolean it replaces existed only to rebuild what was already there (-16 B). `removeEventListener` is a silent no-op when the handler or the capture flag differ, and the two calls had disagreed harmlessly (`{}` against `undefined`) - which forced the test comparing them to compare capture flags rather than arguments. A comparison weakened to pass is a comparison that will not catch the thing it is for: add `{ once: true }` to one side and removal stops matching, with no error. The resulting identity assertion is labelled a STRUCTURAL PIN, stricter than the platform requires: its mutation (teardown passes a copy) is an equivalent mutant, so unlike the others it shows only that the assertion is strict, and a refactor that copies for a reason may relax it. | src/directives.ts; tests/directives-vapor-fixture.test.ts; .probes/mutate.sh; rc-alignment-work.md s34 |
| 2026-09-21 | BACKFILLED with s34. Probes - a cycle's throwaway measurements - live in `.probes/` at the repository ROOT, gitignored, run through a committed `vitest.probes.config.ts`. The first arrangement put them under `tests/` so vitest's anchored `include` could reach them, and then every tool that walks that directory found them and wanted its own exclude: vitest (a probe is usually written to fail, so it failed the suite), biome (it linted a probe's unused import and failed lint:check), check-ascii, with the typecheck include next. Three excludes to hide a directory from tools that had no business seeing it. A standalone config can include any path, so the reason for `tests/` had already evaporated; at the root all three excludes are gone. The config sets `silent: false` AND `disableConsoleIntercept: true` - the first alone produced a clean silent pass, because vitest routes console output to the reporter - so a probe no longer has to fail to print. Untracked files survive a checkout, so the directory is parked before gating historical commits. | vitest.probes.config.ts; vitest.config.ts; biome.json; scripts/check-ascii.mjs; .gitignore; rc-alignment-work.md s34.4 |
| 2026-09-21 | BACKFILLED with s34. A tooling gap, measured and not ours: vue-tsc resolves an IMPORTED directive against its .d.ts and type-checks the template call passing the ARGUMENT as a raw string - the shape #15490 replaced - so the idiomatic `import { vcCommandVapor as vVc }` in `<script setup>` fails `vue-tsc --noEmit` against correct code and a working runtime. Isolated three ways: a directive declared LOCALLY in the SFC is not checked at all and passes; the same directive imported fails at the argument slot; annotating it as Vue's own `VaporDirective` fails too. vue-tsc 3.3.11 is the latest. Upstream's dts-test covers the RUNTIME call shape only; template checking lives in vuejs/language-tools, a different repository, so the change was never checked against it, and no existing issue covers the Vapor case (language-tools#5381 is tsgo support, and closed). examples/vapor-sfc therefore registers the directive app-wide - which passes because vue-tsc then has no declaration to check. That is opting OUT of checking, not satisfying it, and it is a workaround rather than the better style. `npm run check:tsc-gap` asserts the SPECIFIC error (TS2345 naming a getter parameter type), with three outcomes so that an unrelated failure or a silent pass cannot read as "fixed"; when it goes red the example and the JSDoc return to the local-binding form and the script is deleted. | examples/vapor-sfc/src/main.ts; scripts/check-vue-tsc-directive-gap.mjs; package.json; rc-alignment-work.md s34.6 |
| 2026-09-21 | `rc-alignment-work.md` is DEV-ONLY and permanent, decided by the owner after the rc.9 sync deleted it unread. It is the per-RC checklist and the cycle record, tracked in this repository and never deleted at a cycle close; its own line 8 used to read "commit it or delete it when the cycle closes", which is how `1d51ed2` came to remove it while making the worktree 1:1 with upstream (restored by `rc9/17`). Verified it cannot ship rather than assumed: `package.json` "files" is an allowlist (bin, dist, src, scripts, ROADMAP.md, CHANGELOG.md, LICENSE), there is no `.npmignore`, and `npm pack --dry-run` lists 320 files with zero occurrences of it. "Remove" applies to what is deployed or published; a sync with upstream removes it and it is restored afterwards. Line 8 now says all of this in place of the sentence that invited the deletion. | rc-alignment-work.md line 8; package.json "files"; rc-alignment-work.md s34.8 |
| 2026-09-21 | Vue 3.6.0-rc.9 `80b3a046` (compiled custom directives emitted at the END of a block, after props, children and v-model) flipped which click listener registers first on an element carrying BOTH a template `@click` and `v-vc:command`: the directive ran first on rc.8 and runs second on rc.9. OWNER RULING: the three guards in `buildHandler` - the internal in-flight flag, the DOM `disabled` PROPERTY and the `aria-disabled` ATTRIBUTE - STAY. It is deliberate since v1.6.0, mirroring Vue #14948 for the DIRECT listener this directive attaches, and one test pins it - verified by deletion. Measured per element type while documenting it, correcting a tidier reading: `.disabled` is a real boolean only on form controls and `undefined` on `<a>`/`<div>`/`<span>`, but an assignment there is an expando that reads true, so the property guard fires on every tag - the two lines are not a form-control/other split, and the aria line is simply the only signal the non-form tags have. A template handler that disables the element or calls `stopImmediatePropagation()` therefore vetoes the dispatch, and that is the contract working; what changed is who runs first, not what the guard does. Documented rather than fixed, because the scope is exactly one shape no example or recipe uses (feature-directives.html states it has no click handler at all). MEASURED, not inferred: both compilers over one template (rc.8 emits `_withVaporDirectives` then `_on`, rc.9 the reverse), the order isolated on one runtime, and one element per renderer - which shows rc.9 makes Vapor MATCH vDOM, since Vue patches props before a directive `mounted`, and that `.delegate` is untouched (its listener is on the document, which is also why plain `stopPropagation()` vetoes that mode alone). The fixture case is COVERAGE, labelled as such: rc.8 is not installed to fail against. | src/directives.ts (both JSDoc sites); tests/directives-vapor-fixture.test.ts; CHANGELOG v1.22.0 upgrade note; rc-alignment-work.md s34.19 |
| 2026-09-21 | The rc.9 cycle's TV-N batch ACCEPTED as TV at cycle close by the owner: 40 rows of `v3.6.0-rc.8..v3.6.0-rc.9` closed on package plus a symbol grep against `src` rather than on a full diff read - releases, merges, chores and dependency bumps, compiler-sfc internals, compiler-only codegen, one test-only row, and two commits whose contact a src comment DECLARES to be pass-through. Conditioned by the owner on the three rows that ship inside a consumer bundle being read in full first, which they now are: `599f35bb` (reactivity array instrumentations - unreachable from src, which never calls `readonly()`, and the default `signal()` is `shallowRef` so `useCommandState` holds a raw array and never enters `reactiveReadArray`), `718f782b` (shared `looseEqual`, 0 hits in src) and `b9456cb4` (shared `normalizeStyle`, 0 hits; this package writes no style binding). Those three leave the batch for V, so the acceptance covers 37 rows with nothing left to find. The five Suspense rows s34.15 returned are explicitly NOT covered and are moot, all five having been read. This supersedes nothing: s34.15 recorded an acceptance before it was given and s34.16 retracted it; this row is the first time it has been given. | rc-alignment-work.md s34.24 (the sign-off and the three rows); s34.18 point 4 (the listing); s34.20 (the count correction); s34.16 (the retraction it replaces) |
| 2026-09-21 | `createDirectivePlugin()` installed on a Vapor app warns in DEV and names the fix (owner: option C of three measured; the owner approved the OPTION, not a byte figure). The plugin registers vDOM OBJECT directives; from Vue 3.6.0-rc.9 a Vapor template skips a non-function directive with its own warning (#15489), and before that called the object and threw at mount - either way every `v-vc:command` is inert, and Vue names only the symptom. Detection needs no `vue` import: measured, a Vapor app carries an own `vapor` key and a vDOM app does not, while `_context` has the identical key set on both. DECLINED: option A, auto-registering `vcCommandVapor` (+84 B raw / +11 brotli) - it silently changes what `.use()` does and, in the measured shape, drops `v-vc:payload` / `v-vc:optimistic`, which have no Vapor port, so it would half-work and hide the gap it should surface; option B, throwing (+170 / +46) - a hard install failure against this file's own precedent, where `warnUnwired` and the below-floor-Vue guard both warn and continue. SQUEEZED BEFORE LANDING, five builds, each measured against a no-guard build: the `if (DEV && app.vapor)` branch alone +34 raw / +8 brotli in the one empty-branch shape built - NOT established as a floor, no cheaper shape was tried and the +34 raw is unexplained for a branch that should dead-code-eliminate - then the message - full sentence with the subpath +162 / +37, without the subpath +132 / +27, without the plugin name +123 / +27, and the shipped form, which names the fix alone, +94 / +21. The first cut also carried the payload/optimistic asymmetry and measured +352 / +102; it was cut because Vue already names the symptom and the asymmetry is documented in four places. SHIPPED +94 raw / +21 brotli on `dist/directives2.js`, an unbudgeted file; all three IIFEs and the esm-treeshake consumer are byte-identical, which matters because all six IIFE budgets currently sit at exactly 0 bytes of headroom. The test asserts OUR message text and silence under `NODE_ENV=production`, not merely that a warning fired - Vue's own #15489 warning fires in the same run, so the test drives a compiled template, asserts both appear, and shows the discrimination is real; armed by deleting the guard, which fails it on its own assertion. | src/directives.ts; tests/directives-vapor-fixture.test.ts; CHANGELOG v1.22.0; rc-alignment-work.md s34.21 (the proposal), s34.25 |
| 2026-09-22 | THE DIRECTIVE SELECTOR MOVED OUT OF THE ARGUMENT AND INTO THE NAME: `v-vc:command` became `v-vc-command`, and `app.directive('vc', ...)` became `app.directive('vc-command', ...)`. The other two names were ALREADY hyphenated - `vc-payload`, `vc-optimistic` - so exactly one registration was renamed and the rest of the change is deletion. Vue's argument is a PARAMETER slot, dynamic and reactive since #15490; this file used it as a SELECTOR, which is a category error, and the rc.9 defect was downstream of it: comparing a getter to a string made `v-vc:command` a dead control in every compiled Vapor template with 2,431 tests green. DELETED with the rename: the `argument()` call, the DEV non-getter guard, the below-floor Vue version warning that existed only to explain it, both `binding.arg` guards, and the documented `v-vc:[kind]` latch - unreachable once no argument exists. DECLINED, by the owner: a deprecated `v-vc:command` alias for one release, because the alias would keep the whole machinery whose removal is the point, leave two spellings, and turn a saving into a cost against budgets at zero headroom; there are no users. MEASURED on a Vite production consumer bundle of `./directives`, brotli q11, the same harness that priced the prototype: 21,722 / 7,126 before, 21,335 / 6,982 after = -387 raw / -144 brotli. The prototype predicted -147 and reproduced EXACTLY at the code-only point (6,979); the missing 3 bytes are the two DEV message strings carrying the notation, which the prototype did not touch - identical in length, 3 bytes worse under brotli. The docblock rewrite measured 0/0, confirming comments do not reach the bundle. All three IIFEs byte-identical at every point, which is the control: none carries directives code, so any movement would have meant a leak. FAILS-BEFORE: 40 tests across 4 files, against the 0 that #15490 - a semantically identical break in the same directive - managed to fail. MEASURED CONSEQUENCE, not predicted by the plan: the reshape CLOSES the vue-tsc template gap for this directive. vue-tsc type-checked the ARGUMENT as a raw string and rejected the imported-binding form; with no argument there is nothing to mis-check. A/B with a control in the example's own project: colon form TS2345, hyphen form exit 0, and a deliberate error in the same probe still reports TS2339 - so the template is genuinely being checked and the difference is real. The tool has NOT caught up; our usage simply no longer has the shape that triggered it. | src/directives.ts; tests/directives.test.ts; tests/directives-vapor-fixture.test.ts (two new COMPILED-notation cases, plan section 4b); tests/compile-vapor.ts (`compileVdom`); scripts/check-example-directive.mjs; scripts/check-vue-tsc-directive-gap.mjs; examples/vapor-sfc; CHANGELOG v1.22.0 |
| 2026-09-22 | The vue-tsc template gap check DELETED, and `examples/vapor-sfc` returned to the imported-binding form (owner). The gap needed a directive ARGUMENT - vue-tsc type-checked it as a raw string, the pre-#15490 shape - and the v1.22.0 reshape removed the argument, so `v-vc-command` gives it nothing to mistype. MEASURED with four arms, after a first A/B that was INVALID because it used the binding `vVc` on both arms and `v-vc-command` resolves to `vVcCommand`, so the hyphen arm had no declaration to check and passed for the same reason the workaround passed: colon+vVc TS2345, hyphen+vVcCommand clean, hyphen+deliberate-error TS2339 whose message enumerates vVcCommand's signature and so proves the binding resolved, hyphen+wrong-binding clean. Corroborated by the example's own `vue-tsc --noEmit` build with the binding used and the control dispatching. SETTLED BY HISTORY, not argument: `f902a15` (rc9/12) created that example's directive usage AND chose app-wide registration because of the gap, so there was no pre-existing style to preserve. DECLINED: re-pointing the script at the colon form as an upstream tracker - it would track a shape this package no longer uses and could only go red for a reason that cannot affect it, an assertion that cannot fail in mirror image; and leaving the gate red, because a normalised red stops being read. The upstream bug is NOT fixed and the knowledge is kept: the CHANGELOG states that the gap is unreachable through `v-vc-command` because there is no argument to mistype, which is both evidence that the reshape removed a class of upstream exposure and the cost any future argument-based notation must answer. SEPARATELY FOUND while doing it: the local binding name changes, `vVc` -> `vVcCommand`, and an import left on the old alias compiles, type-checks and mounts nothing - the same silent dead control #15490 produced. It happened in this very change and was caught by `npm run check:example` alone. | scripts/check-vue-tsc-directive-gap.mjs (deleted); package.json; scripts/gate.mjs; .gitignore; examples/vapor-sfc/src/{main.ts,CartPanel.vue}; src/directives.ts; docs/whitepaper.md; CHANGELOG v1.22.0; rc-alignment-work.md s34.26 |
| 2026-09-22 | SECTION 4a DECIDED, option B2 (owner): `v-vc-payload` and `v-vc-optimistic` PORTED to Vapor as `vcPayloadVapor` / `vcOptimisticVapor`, order-independent. The asymmetry was the defect - the same public spelling meaning different things per renderer is the shape that produced the `v-vc:payload` drift - and B removes it by levelling UP. Three options were sized first, no code: A drop the two vDOM-only directives (-419 raw / -76 brotli), B1 port them order-dependently (+671 / +115), B2 port them order-independently (+493 / +119 predicted, +500 / +125 shipped). DECLINED A, and the owner corrected the plan for offering only A-or-keep: it fails the no-removal-for-zero-consumers rule because its substitute is NOT equivalent - measured, `data-vc-payload` is JSON so a Date arrives as a string, a Map as `{}`, a class instance as a plain object and a function and an undefined key vanish; and optimistic has no substitute at all, so a Vapor consumer must abandon the directive and forfeit vc-loading, disable-while-busy, the re-entrancy guard, the timeout and the modifiers, and cannot recover them with an @click because 80b3a046 lets that handler veto the dispatch. CORRECTED BY MEASUREMENT: A is NOT weak on reactivity - a bound `data-vc-payload` does track a changing payload on both renderers; it is weak on FIDELITY. DECLINED B1 for four brotli bytes: a compiled template applies directives in source order and mountCommand() clears the element first, so B1 loses a payload written before the command, silently. B2 makes that impossible via a weak pending slot rather than warning about it. STAGED, not argued: reducing the shipped port to B1 fails exactly the payload-before-command case, from two independent injections. Bytes on ./directives, which is unbudgeted; all three IIFEs byte-identical; .d.ts +3,238 B, which no consumer bundles. The rc9/57 alias rule carries: the bindings are `vVcPayload` and `vVcOptimistic`, a shorter alias is silent in a production build, and check:example now discriminates a dead payload (qty 1) from a live one (qty 3). | src/directives.ts; tests/directives-vapor-fixture.test.ts; scripts/check-example-directive.mjs; scripts/gate.mjs; examples/vapor-sfc/src/CartPanel.vue; docs/whitepaper.md; ROADMAP.md; README.md; examples/feature-directives.html; CHANGELOG v1.22.0; rc-alignment-work.md s34.27 |
