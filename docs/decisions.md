# Decision log

Append-only, dated, past tense by construction. One row per stage transition,
never per activity. This file records what was decided and where the evidence
lives; it never describes current behaviour, so it cannot drift and must never
be patched. Current truth lives in exactly three places: the code, the tests,
and the stamped docs. Anything else here is history.

The workflow this file belongs to (adopted 2026-08-28): proposals carry their
judgment half as a paragraph in the relevant planning doc and their measurable
half as acceptance assertions, armed at spike stage; promotion renames the
acceptance file into the permanent suite; agent reports and audit verdicts
travel in commit messages; retired documents are deleted and retrieved through
git history, not kept live to rot.

| date | decision | evidence / where |
| --- | --- | --- |
| 2026-08-28 | Posture: pre-v2 maturation recorded by the decision owner. The RC window is the runway; the freeze is evidence pressure (the v1.17.0 template: measured cost, what it buys, fixtures), not a calendar. Supersedes the park-to-stable defaults in earlier drafts. | docs/plan-vapor-composition.md section 7 |
| 2026-08-28 | Vapor outlet Phase 1 spike PASSED: -20.13 KB brotli vs a re-derived interop baseline against a >= 20 KB accepting bar, least favourable of four method variants, four behaviours passing against an executed production build, no interop binding retained. Audited and reproduced run-for-run. | superseded spike (git history); method survives in tests/vapor/vapor-outlet-size.test.ts |
| 2026-08-28 | Vapor outlet promoted: vapor-chamber/router/vapor ships experimental in the next minor. One additive error code (mode_mismatch). Shipped margin 20.03 KB over the bar; the per-run guard fails loudly on erosion and a failure is a decision trigger, not a threshold to raise. Two recovery levers measured and DECLINED at acceptance (plain Error for routerError, ~77 B; dropping the DEV gate, ~35 B), recorded in the guard's header. | src/router/vapor.ts; tests/vapor/vapor-outlet*.test.ts; tests/router/vapor-boundary.test.ts; ROADMAP version-targets row; CHANGELOG Unreleased |
| 2026-08-28 | Store direction: a Pinia-like store is rc-independent (stable 3.5-era API or the signal shim) and 1.x-eligible under the maturation posture, gated on a deliberate reversal of whitepaper section 6 recorded in the same commit that documents the partition rule. Pinia 4.0.3 read at source: a packaging major (ESM-only, devtools-api v8, Nostics), no Vapor or signals work; patterns adopted by reference, its action() mini-bus replaced by the real bus. | docs/plan-vapor-composition.md sections 3, 4, 7, 8 |
| 2026-08-28 | Packaging: a separate satellite package for the outlet (and any future store) considered and REJECTED; cost isolation is an import-graph property the subpath already delivers, and semver quarantine already exists in-package (experimental status, v1.11.0 precedent). | docs/plan-vapor-composition.md section 6 |
| 2026-08-28 | Style: ASCII-only typography adopted project-wide by the owner (no em/en dashes, arrows, ellipsis, times sign, curly quotes). Measured honestly: 245 B raw / ~95 B compressed across dist strings, ~6 KB npm tarball; the rule is the primary reason, the bytes are the dividend. Guard exists; the one-pass sweep and lint:check wiring are pending. | scripts/check-ascii.mjs |
| 2026-08-28 | Workflow: git adopted as the substrate (baseline 30edfae); this decision log adopted in the whitepaper alignment-log form; acceptance-assertions-at-spike adopted as the RFC replacement for measurable claims. The outlet RFC, fully promoted, is deleted in the commit following this row and retrievable from git history. | this file; git log |
| 2026-08-31 | Size metric: a second splitting-aware pass adopted in measure-size.mjs (first load / on demand columns, attribution by import-statement reachability from the entry chunk, not esbuild's entryPoint flag - which is stamped on dynamic-import chunks and would invert the answer). Historical single-file column retained unchanged for cross-release comparability. Trigger: the no-splitting pass inlines internal dynamic imports plus wrapper, charging a penalty for deferral. | scripts/measure-size.mjs; docs/BUNDLE-SIZES.md |
| 2026-08-31 | Router http client removed from the core: http and fetchBlade become ordinary options, the in-box pair ships as vapor-chamber/router/remote (routerHttp, bladeFetcher; bladeRoot moved onto the fetcher). Breaking for { url } tables and blade rows, two-line migration, taken now under the maturation posture - the RC window is the runway, and the cost of this break only rises. An intermediate lazy-import variant was measured and superseded in the same session: it fixed startup cost but left the dependency in the graph and the decision with the callee. ./router 12.4 -> 9.6 KB brotli; boundary enforced like the renderer boundaries. | src/router/remote.ts; tests/router/remote-boundary.test.ts; docs/router.md; CHANGELOG Unreleased |
| 2026-08-31 | revalidateRoutes stage 1 (acceptance before implementation): criteria committed as tests/revalidate-routes.test.ts - 6 assertions on the composition surface today, 13 it.todo armed at implementation. PLACEMENT decided bus-side with the router passed STRUCTURALLY: src/router imports nothing from the bus and src/index.ts nothing from the router, so a router-adjacent subpath would create a router -> bus edge that does not exist, to host a plugin installed on the bus. REVALIDATING decided as the plugin's own signal, which CORRECTS a plan claim: section 4C says "isRevalidating drives the UI for free" and it cannot - router.isRevalidating is Readonly and its only writer, the engine's trackRevalidation, is absent from the Router type, so driving it needs a cast or new router surface, both out of scope by the brief. | tests/revalidate-routes.test.ts; docs/plan-vapor-composition.md section 4C |
| 2026-08-31 | revalidateRoutes shipped (experimental, next minor): a bus plugin composing runLoaders + currentRoute + setRouteData, zero new router capability. THREE decisions revised by evidence rather than argued: placement moved bus-side -> router-side at stage 2 (runLoaders has no public substitute, the bus contributes one inline function shape - deep one way, shallow the other); then subpath -> the EXISTING router entry, because minting router/revalidate added a Vite build entry that re-chunked shared code and dropped the outlet guard 20.02 -> 19.90 with the outlet itself unchanged, and a subpath isolates no cost when the module imports nothing the core lacks. `loaders` is an explicit argument: createRouter closes over its preset, so a two-arg form would run a second http client and cache. | src/router/revalidate.ts; tests/revalidate-routes.test.ts; docs/router.md |
| 2026-08-31 | Guard-and-core review pass, restraint applied as review rather than addition. Findings: check-line-citations was blind to 73 of 2524 test titles (double/backtick quotes, .each/.skipIf chains) and the hole was LIVE - six stale citations were sitting behind it, each in a double-quoted title precisely because the title quoted a string. BUNDLE-SIZES promised hard ceilings over the whole document; check-size budgets 3 IIFE files and none of the 24 ESM rows, which is why size:check stayed green while ./router moved 12.4 -> 13.1 -> 9.6. command-bus.ts's header quoted ~3.6 KB against a measured 4.2, and CommandMeta.id documented crypto.randomUUID as the default when the default is a monotonic counter - contradicting the ts field eight lines above it. Guards are now believed only as far as their scope, which is stated. | scripts/check-line-citations.mjs; scripts/measure-size.mjs; src/command-bus.ts |
| 2026-08-31 | Prose guards adopted: scripts/check-doc-claims.mjs wired into lint:check (orphaned docblocks, documented defaults whose literal is absent from the file, release status in comments - the last per the owner rule that release state belongs to CHANGELOG.md and a bare version number is enough in source). check-ascii extended to invisible characters (ZWSP, word joiner, BOM, NBSP; joiners excluded on biome noMisleadingCharacterClass). Trigger: a read pass found 14 defects, all in prose and none able to fail a test; the docblock check was found by reading one file and then caught two more by sweep, one of them in a passage read attentively the same hour. Scope stated in the header: these check that a claim is ATTACHED and its literal EXISTS, never that it is true. | scripts/check-doc-claims.mjs; scripts/check-ascii.mjs; CONTRIBUTING.md |
| 2026-08-31 | Store stage 1 (acceptance before implementation), and a correction: Vue 3.6 stable was recorded as a GATE on the composition plan and it is a DEADLINE - section 7 posture is that the RC window is the runway, item 4 at stable is re-measure/promote/stabilize, and section 7 item 2 marks the store rc-independent. The store gate is one sentence of whitepaper section 6 about PACKAGE scope (a state layer as a fourth source of truth), not its architectural claim (the bus stores no state), which the store preserves. Stage 1 also falsified a section 3 pattern: injection-first resolution on hasInjectionContext() is unreachable (module-private in chamber.ts, no accessor) and undesirable (chamber reaches Vue by the runtime probe behind both shipped prod-only bugs) - the store imports from vue statically like src/router, which also settles it as a subpath. | tests/chamber-store.test.ts; docs/plan-vapor-composition.md |
| 2026-08-31 | Store shipped (experimental, next minor): defineChamberStore on the ./store subpath, importing vue and nothing else. The whitepaper section 6 reversal is recorded in section 6 itself and is PACKAGE scope only - the bus still stores nothing, a store owns its own shallowRef, and the competition argument is what failed: Pinia's action()/$onAction mini-bus exists because no bus is underneath it, so here the store is 0.6 KB of the existing plugin catalogue applied to state rather than a fourth source of truth. ONE decision revised by measurement: the bus was a DEFAULTED argument (getCommandBus()) and is now REQUIRED. The default contradicted both of its neighbouring docblocks - it imported chamber.ts, so the module written to avoid the runtime probe executed it at load, and it reached for the module global that the per-bus registry exists precisely to avoid, handing every SSR request the same stores. Invisible to a green suite; caught only by the module measuring 16.7 KB raw / 5.4 KB brotli against the section 2 estimate of 1-2 KB. Required: 1.4 KB raw / 0.6 KB brotli. The boundary test written to pin this was itself inert on first write - under happy-dom import.meta.url is an http://localhost URL, so existsSync answered false every run and the assertion could not fail; it is armed by injection now. url fields (4B) take the router structurally, so a store without them never pulls it into the graph. | src/store.ts; tests/chamber-store.test.ts; docs/store.md; docs/whitepaper.md section 6; CHANGELOG Unreleased |
