<!-- GENERATED by `npm run docs` (scripts/generate-api-docs.mjs) - do not edit by hand. -->
# `vapor-chamber/vitest/mcp`

Source: [`src/vitest-mcp.ts`](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts). 10 public exports.

```ts
import { ... } from 'vapor-chamber/vitest/mcp';
```

## Contents

**Functions:** [`coverageGaps`](#coveragegaps) [`createVitestMcp`](#createvitestmcp) [`serveVitestMcp`](#servevitestmcp)

**Interfaces:** [`VitestMcpFailure`](#vitestmcpfailure) [`VitestMcpGap`](#vitestmcpgap) [`VitestMcpOptions`](#vitestmcpoptions) [`VitestMcpRun`](#vitestmcprun) [`VitestMcpServer`](#vitestmcpserver)

**Variables:** [`VITEST_MCP_ACTIONS`](#vitest_mcp_actions) [`VITEST_MCP_SCHEMA`](#vitest_mcp_schema)

## Functions

### coverageGaps

**Function** - [src/vitest-mcp.ts:149](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L149)

```ts
coverageGaps(report: Record<string, FileCoverage>, root: string) => VitestMcpGap[]
```

Gaps from an Istanbul `coverage-final.json`, files with none left out.

### createVitestMcp

**Function** - [src/vitest-mcp.ts:197](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L197)

```ts
createVitestMcp(options?: VitestMcpOptions) => Promise<VitestMcpServer>
```

Start the server: a warm Vitest on `root`, a file watcher, and a schema bus
whose handlers are the three tools. Serve `bus` with `createMcpHandler` or
`serveMcpStdio`, passing {@link VITEST_MCP_ACTIONS} (or fewer) as `actions`.

### serveVitestMcp

**Function** - [src/vitest-mcp.ts:295](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L295)

```ts
serveVitestMcp(options?: VitestMcpOptions) => Promise<() => Promise<void>>
```

{@link createVitestMcp}, served over stdio with every tool allowed. Resolves
to a `stop()` that detaches from stdin and closes Vitest.

## Interfaces

### VitestMcpFailure

**Interface** - [src/vitest-mcp.ts:78](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L78)

```ts
export interface VitestMcpFailure {
  file: string;
  test: string;
  message: string;
}
```

One failed test, as `runTests` reports it.

### VitestMcpGap

**Interface** - [src/vitest-mcp.ts:96](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L96)

```ts
export interface VitestMcpGap {
  file: string;
  /** Uncovered statement lines, as ranges: `"3, 8-9"`. */
  uncoveredLines: string;
  uncoveredFunctions: string[];
  uncoveredBranches: { line: number; type: string; uncoveredArms: number[]; arms: number }[];
}
```

One source file with something uncovered, as `getCoverageGaps` reports it.

### VitestMcpOptions

**Interface** - [src/vitest-mcp.ts:70](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L70)

```ts
export interface VitestMcpOptions {
  /** The project root. Default: the working directory. */
  root?: string;
  /** A Vitest config file, as `--config` takes it. Default: Vitest's own lookup from the root. */
  config?: string;
}
```

### VitestMcpRun

**Interface** - [src/vitest-mcp.ts:85](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L85)

```ts
export interface VitestMcpRun {
  files: number;
  passed: number;
  failed: number;
  skipped: number;
  failures: VitestMcpFailure[];
  /** Errors outside any test (a file that failed to load, an unhandled rejection). */
  errors: string[];
}
```

What `runTests` returns.

### VitestMcpServer

**Interface** - [src/vitest-mcp.ts:105](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L105)

```ts
export interface VitestMcpServer {
  bus: ReturnType<typeof createAsyncSchemaCommandBus<BusSchema>>;
  close(): Promise<void>;
}
```

A running server: the bus to serve, and `close()`.

## Variables

### VITEST_MCP_ACTIONS

**Variable** - [src/vitest-mcp.ts:68](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L68)

```ts
VITEST_MCP_ACTIONS: string[]
```

Every tool name, the default allow-list.

### VITEST_MCP_SCHEMA

**Variable** - [src/vitest-mcp.ts:51](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/vitest-mcp.ts#L51)

```ts
VITEST_MCP_SCHEMA: BusSchema
```

The tools, as a bus schema: `tools/list` is derived from it.
