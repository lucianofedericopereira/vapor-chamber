<!-- GENERATED by `npm run docs` (scripts/generate-api-docs.mjs) - do not edit by hand. -->
# `vapor-chamber/devtools`

Source: [`src/devtools.ts`](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/devtools.ts). 1 public export.

```ts
import { ... } from 'vapor-chamber/devtools';
```

## Contents

**Functions:** [`setupDevtools`](#setupdevtools)

## Functions

### setupDevtools

**Function** - [src/devtools.ts:47](https://github.com/lucianofedericopereira/vapor-chamber/blob/main/src/devtools.ts#L47)

```ts
setupDevtools(bus: Observable, app: unknown) => () => void
```

Connect a command bus to Vue DevTools.

- Adds a **Commands** timeline layer: every dispatch appears as an event,
  green for success, red for error.
- Adds a **Vapor Chamber** inspector panel: browse recent commands,
  inspect target/payload/result of each one.

| parameter | description |
|---|---|
| `bus` | A CommandBus or AsyncCommandBus instance to observe. |
| `app` | The Vue app instance (passed to setupDevtoolsPlugin). |

**Returns:** Unsubscribe function - call it to detach from the bus.

```ts
import { createApp } from 'vue';
import { getCommandBus, setupDevtools } from 'vapor-chamber';

const app = createApp(App);
setupDevtools(getCommandBus(), app);
app.mount('#app');
```
