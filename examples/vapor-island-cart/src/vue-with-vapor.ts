// Synthesized with-vapor esm-bundler entry. Vue ships with-vapor only as a
// pre-bundled esm-browser dist (no bundler build at rc.3), which tree-shakes
// badly and, in the non-.prod variant, ships dev Vue to production. These two
// packages do ship bundler builds. Aliased as `vue` in vite.config.ts.
export * from '@vue/runtime-dom';
export * from '@vue/runtime-vapor';
