{
  "name": "vapor-chamber-example-vapor-island-cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Light-DOM Vapor custom-element island cart for vapor-chamber. Run with `npm install && npm run dev`.",
  "scripts": {
    "ensure-lib": "node ../ensure-lib.mjs",
    "predev": "npm run ensure-lib",
    "prebuild": "npm run ensure-lib",
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vapor-chamber": "file:../..",
    "vue": "^3.6.0-rc.7"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  }
}
